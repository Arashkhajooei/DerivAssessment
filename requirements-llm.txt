# Optional: only needed to run the judge's `live` backend against a real
# API. The pipeline runs end to end without these via the `cache` and
# `stub` backends, so they are deliberately not hard requirements.
#
# Install whichever matches `judge.provider` in config.yaml:
openai==1.59.6
# anthropic==0.42.0
